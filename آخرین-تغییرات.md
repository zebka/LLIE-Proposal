# آخرین تغییرات — پیش از تولید ورد نهایی

**آخرین به‌روزرسانی:** 2026-08-31 | **نسخه فعلی:** 0.3 | **خروجی:** `PhDProposalform-Completed.docx` (۵۵ مرجع، ۱۷ شکل، ۲ جدول تحلیلی)

---

## نسخه ۰.۳ — ممیزی دوم (بازبینی محتوایی خارجی) + تطبیق با فرم ویرایش‌شده

### اعمال‌شده

**رفرنس‌ها**
1. Zero-DCE++ (TPAMI): صفحات **4225–4238** و سال **2022** (راستی‌آزمایی dblp؛ DOI افزوده شد)؛ کلید ← `li2022zerodcepp`
2. سروی [1]: یادداشت «First posted 2022; v5 Jan 2024»
3. OWDiff: یادداشت «Advance online publication»

**ادعاهای علمی**
4. ادعای DDPM («توزیع... روی میلیاردها نمونه») بازنویسی شد — نسخه‌های متن‌به‌تصویر وب‌مقیاس با استناد به [cho2024zeroshot] تفکیک شدند
5. شکاف گرگ‌ومیش: **ExDark** (دارای صحنه‌های گرگ‌ومیش) صریحاً به‌رسمیت شناخته شد و «شامل صحنه» از «بنچمارک با پروتکل ارزیابی اختصاصی» تفکیک گردید
6. همه عبارت‌های «نخستین/اولین» به صیانت‌نامه «تا حد اطلاع نویسندگان / در مرور انجام‌شده» تبدیل شدند (لایه‌های نوآوری + هدف جزئی ۴)
7. لحن: «بدترین ترکیب ممکن»، «مهیب‌ترین»، «پرریسک‌ترین» → بی‌طرف علمی
8. PairLIE: توصیف با سازوکار (یادگیری فقط از نمونه‌های کم‌نور جفت‌شده) به‌جای برچسب مناقشه‌برانگیز
9. تفکیک صریح **zero-reference ≠ zero-shot** در ۱-۳-۱ افزوده شد

**شکل‌ها**
10. شماره‌گذاری ترتیبی ۱–۱۷ طبق ترتیب ظهور در سند؛ درون‌نویسی‌ها هماهنگ شد
11. دو کپشن یتیم اضافه شد: شکل ۳ (رده‌بندی سلسله‌مراتبی سروی) و شکل ۸ (معماری LLFlow) — قبلاً در figures-plan بودند اما کپشن نداشتند
12. نام فایل‌های PNG و جدول figures-plan با شماره‌های جدید هماهنگ شد

**فرضیه‌ها**
13. `mAP@0.5` استانداردسازی شد؛ «+۵ واحد درصد مطلق» تعریف شد (مطلق نه نسبی)؛ بازه روشنایی ۰/۴–۰/۶ = «آستانه پیشنهادی پژوهش»

**فرم (تطبیق با نسخه ویرایش‌شده دستی دانشجو)**
14. `fill_form.py` به ساختار جدید فرم تطبیق یافت: پر کردن **فقط خانه‌های خالی** (عنوان فارسیِ دست‌نویس دانشجو با «پیشین (Prior)» **حفظ شد**) + پاک‌سازی و بازپرانی خانه‌های تولیدی + بازسازی جدول‌های ریسک/مقایسه که هنگام ذخیره ورد از سلول‌ها جدا شده بودند + به‌روزرسانی جدول زمان‌بندی مستقل (فاز ۳ «پالایش»، فاز ۴ «ناشناس‌سازی و در صورت احراز شرایط انتشار»)
15. بکاپ نسخه دستی: `PhDProposalform-USER-EDITED-BACKUP.docx`

### رد‌شده (ساختگی/خوانش نادرست بازبین — با سند)
- «LLIE-GPP» و «shot-zero»: در هیچ فایلی وجود ندارند (grep تأیید)
- «دو بار شکل ۶ و شکل ۱۰»: شماره‌ها یکتا بودند؛ ایراد واقعی «ترتیب غیرترتیبی + دو شکل بدون کپشن» بود که اصلاح شد
- «شکل ۱ انتساب اشتباه»: تصویر واقعاً Fig.1 مرجع [1] (سروی) است که خودش تصاویر SICE را نمایش می‌دهد
- جایگزینی کامل واژگان انگلیسی با فارسی (صفرنمونه، دانه تصادفی، خط مبنا...): برای پروپوزال مهندسی مناسب نیست؛ قاعده: «معادل فارسی (English)» فقط در نخستین کاربرد

---

## نسخه ۰.۲۵ — استخراج و کپشن شکل‌ها

1. `extract_figures.py`: استخراج خودکار **۱۸ شکل با ۶۰۰ DPI** از ۱۴ مقاله arXiv (پیدا کردن کپشن → خوشه تصویر/وکتور → کراپ) + نسخه fullpage با ۳۰۰ DPI به‌عنوان پشتیبان هر شکل؛ خروجی‌ها در `figures/` (در گیت مستثنا؛ گزارش: `figures/extract-report.txt`)
2. همه ۱۵ کپشن به فرم رسمی تبدیل شد: «شکل n — توضیح (برگرفته از Fig.x در مرجع [n])» با شماره مرجعِ خودکار هم‌سو با فهرست مراجع

## نسخه ۰.۲ — ممیزی اول

1. تغییر نام ۳ کلید BibTeX (۱۷ جایگزینی): `wang2026darkdriving`، `aithal2025lenviz`، `islam2024lolistreet`
2. افزودن ۳ لنگر داوری‌شده (۵۲ ← ۵۵ مرجع): `yan2025hvi` (CVPR 2025)، `zhou2025gppllie` (AAAI 2025)، `afifi2021exposure` (CVPR 2021)
3. عنوان بازنویسی شد با تصریح گرگ‌ومیش؛ نسخه دستی نهایی دانشجو روی جلد فرم اعمال شد
4. `fill_form.py`: مپ خودکار ارجاع‌های داخل جدول‌ها + حذف کد مرده
5. رد با سند: «پذیرش ICRA 2026 DarkDriving تأییدنشده» (صفحه arXiv صریحاً پذیرش را اعلام می‌کند)؛ «InterLight بدون تأیید IJCAI» (same)

---

## چک‌لیست دستی قبل از تحویل

| # | کار | وضعیت |
|---|---|---|
| ۱ | نام، شماره دانشجویی، دانشکده/رشته/گرایش، تلفن، ایمیل | ⬜ |
| ۲ | جدول اساتید راهنما/مشاور + امضاها | ⬜ |
| ۳ | تیک «نظری- عملی» در جنبه تحقیق | ⬜ |
| ۴ | تصمیم شکل ۱۶: انتخاب یکی از سه کاندید (LENVIZ/LoLI-Street/DarkDriving) یا ترکیب سه‌تایی، تا کپشن با تصویر بخواند | ⬜ |
| ۵ | جایگذاری ۱۷ شکل از `figures/` در جاهای علامت‌گذاری‌شده؛ شکل ۱۷ = عکس‌های خودت؛ شکل-05 را با fullpage مقایسه کن (کراپ ممکن است تنگ باشد) | ⬜ |
| ۶ | بازبینی بصری نهایی (فونت، شکست صفحه، عرض جدول‌ها) | ⬜ |
| ۷ | تأیید با گروه: آیا فرم رسمی بخش ۶ (زمان‌بندی) مستقل دارد؟ (فعلاً زیر بخش ۴ با عنوان ۴-۶ قرار دارد) | ⬜ |

## قانون به‌روزرسانی (مهم)

**docx را هرگز دستی ویرایش نکنید.** جریان درست:
```
ویرایش فایل‌های md (منبع) → اجرای python fill_form.py → docx بازتولید می‌شود
```
ارجاع‌ها، شماره‌گذاری، RTL و جدول‌ها خودکار اعمال می‌شوند. (استثنا: فیلدهای شخصی و امضاها — دستی.)
